 // int currColIndex = colIndex;
        // vector<int> currRow = A[currRowIndex];
        // while (currColIndex >= 0 && currRow[currColIndex] == 1)
        // {
        //     debug(currColIndex);
        //     currColIndex--;
        // }

        // if (currColIndex < colIndex)
        // {
        //     rowIndex = currRowIndex;
        //     colIndex = currColIndex;
        // }